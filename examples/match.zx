my x = 1900;
my y = (1900 / 2);

extern fn printf(fmt: str, ...) -> int;

fn ok() -> int {
    say "ok()";
    return 0;
}

fn nah() -> int {
    say "nah()";
    return 0;
}

match x {
    y => { ok(); }
    _ => { nah(); }
}
