// Pipe operator demo

import "stdio.h"

extern fn printf(fmt: str, ...) -> int;

fn double(x) -> any { return x * 2 };
fn square(x) -> any { return x * x };

my val = 3 |> double |> square; // square(double(3))
printf("\n3 |> double |> square = %lld\n", val);
