type Vec2 struct {
    x int;
    y int;
};

fn (this *Vec2) getx() -> int {
    return this->x; 
};

my myvec = &Vec2{x:234,y:2344};

extern fn printf(fmt: str, ...) -> int;

printf("%d\n",myvec->getx());
