type abc struct {
    x int;
    y int;
};

fn (thing *abc) getx() -> int {
    return thing->x; 
};

my this_thing = &abc{x:234,y:2344};

extern fn printf(fmt: str, ...) -> int;

printf("%d\n",this_thing->getx());
