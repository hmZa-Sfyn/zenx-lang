@test
fn test_addition() {
    assert 1 + 1 == 2, "math is broken";
}

@test
@args={"n": 5}
@expect=10
fn test_double(n: int) -> int {
    return n * 2;
}

@test
@ignore
fn test_ignored() {
    // this test is skipped
}