mod a {
    fn b(x: int) -> int {
        return int(2.55 * x)
    }

    fn something(x: int, y:int) -> int {
        return x * y;
    }

    @test
    @expect=1900
    fn test_demo() -> int {
        return 1900;
    }
}

say a->b(2);

say a->something(345,34);