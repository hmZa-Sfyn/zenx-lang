-- import statement does not work:
"
(myenv)  hmza@0root  ~/workspaces/hamza/zx-lang/zenx-lang   main ±  ./zxc ./examples/imports_named_mod.zx

 error: unexpected token '/' in expression
   ╭─ ./examples/imports_named_mod.zx:9:9
   │    8  // If mod Logger doesn't exist in that file → compile error!
   │    9  import _/logger (Logger)
   │               ^
   │               hint: check for a missing operator, unmatched parenthesis, or a typo
   ╰─

 error: missing ';' after statement
   ╭─ ./examples/imports_named_mod.zx:9:9
   │    8  // If mod Logger doesn't exist in that file → compile error!
   │    9  import _/logger (Logger)
   │               ^
   │               hint: add a semicolon ';' at the end of this statement
   ╰─

 aborting: 2 error(s)

   fix errors and re-run: zxc ./examples/imports_named_mod.zx

(myenv)  ✘ hmza@0root  ~/workspaces/hamza/zx-lang/zenx-lang   main ±  
"

---

- the mods are not workign ass i expected them!
i want them like this:
```perl

mod abc {
   fn def() -> any {
      say "wasssuppp?";
   }
}

abc->def(); // wasssuppp?


def(); // error!

---

but currently even if user tyoed `def();`, it just works, and hoes no errors, please i want them to only be callable with their mod name present!



```
